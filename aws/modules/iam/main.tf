# This is a placeholder file
# ToDo: Provision IAM user e.g. terraform
# Note that IAM resources are global, not region-specific so consider moving
# this module to 'global' directory, which would be at the same level as
# 'eu-west-2'